fn main() {
    // To use modules from the Rust standard library you have to use the use statement which brings
    // the path of that module into the scope of the program.

    // If we want to use the thread module then we have to do use std::thread at the top of the program
    // then we can use this module.

    // In Rust the prelude automatically imports a list of things into every Rust program (like the String
    // module we've been using), but it does not include the entire Rust Standard Library.
}
