fn main() {
    // When we put something in the heap Rust will give us the location of the piece of memory
    // when we allocate it, we can then store this inside a pointer.

    // Accessing data on the heap is slower than the stack, and so is adding data.
    // The heap does let us dynamically add and remove data which is beneficial.
}
